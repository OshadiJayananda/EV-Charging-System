name: .NET Unit Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup .NET SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'  # change to your .NET version

      # Restore dependencies
      - name: Restore dependencies
        run: cd EvBackend && dotnet restore

      # Build the project
      - name: Build
        run: cd EvBackend && dotnet build --no-restore --configuration Release

      # Run tests
      - name: Run tests
        run: cd EvBackend && dotnet test --no-build --verbosity normal
